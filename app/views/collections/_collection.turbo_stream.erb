<%= turbo_stream.replace dom_id(collection, :sidebar),
                     partial: "collections/collection",
                     locals: {
                       collection: collection,
                     } %>
