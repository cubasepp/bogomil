<nav class="background-primary border-b border-color-primary h-16">
  <div class="container mx-auto px-4 h-full flex items-center justify-between">
    <div class="flex items-center">
      <div class="w-10 h-10 mr-2 flex items-center justify-center lg:invisible">
        <button class="text-blue-400 hover:text-blue-500 focus:outline-none" data-action="click->ui#expandNavigation">
          <%= icon(:bars_3, css: "w-6 h-6") %>
        </button>
      </div>
      <span class="text-2xl font-semibold whitespace-nowrap text-blue-400">
        <%= t(:'app.title') %>
      </span>
    </div>
    
    <div class="flex items-center">
      <button class="text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 focus:outline-none focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 rounded-lg p-2.5"
              data-action="click->ui#toggleDarkMode">
        <%= icon(:sun, css: "w-6 h-6 dark:hidden") %>
        <%= icon(:moon, css: "w-6 h-6 hidden dark:block") %>
      </button>
    </div>
  </div>
</nav>
